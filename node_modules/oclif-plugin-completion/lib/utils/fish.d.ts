import type { Command } from '@oclif/command';
export declare function generateCompletionScriptForFish({ bin, aliases, commands, }: Pick<Command['config'], 'bin' | 'commands'> & {
    aliases: string[];
}): string;
export declare function generateCompletionAliasScriptForFish({ bin, alias, }: {
    bin: string;
    alias: string;
}): string;
export declare function getInstructionsForFish({ bin, shell, aliases, }: {
    bin: string;
    shell: string;
    aliases: string[];
}): string[];
