import Command, { flags } from '@oclif/command';
export declare abstract class CompletionBase extends Command {
    aliases: string[];
    static flags: {
        shell: flags.IOptionFlag<string>;
    };
    init(): Promise<void>;
}
