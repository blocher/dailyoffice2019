import { CompletionBase } from '../../base';
export default class Completion extends CompletionBase {
    static description: string;
    static args: never[];
    static flags: {
        shell: import("@oclif/command/lib/flags").IOptionFlag<string>;
    };
    static examples: string[];
    run(): Promise<void>;
}
